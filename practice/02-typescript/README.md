# Practice 02: TypeScript

Run:

```sh
bun run practice:02
bun run typecheck
```

Edit `answer.ts` until tests and typecheck pass.

Focus:

- object types
- union types
- optional fields
- exact return shapes
